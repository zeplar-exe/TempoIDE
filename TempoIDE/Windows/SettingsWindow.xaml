<windows:ModifiedWindow x:Class="TempoIDE.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:windows="clr-namespace:TempoIDE.Windows"
        xmlns:io="clr-namespace:System.IO;assembly=System.IO.FileSystem"
        xmlns:classes="clr-namespace:TempoIDE.Core.Helpers"
        xmlns:panels="clr-namespace:TempoIDE.Controls.Panels"
        xmlns:subControls="clr-namespace:TempoIDE.Controls.SubControls"
        mc:Ignorable="d"
        Style="{StaticResource ModalWindowStyle}"
        Loaded="SettingsWindow_OnLoaded">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <subControls:TopbarControl Grid.Row="0" Template="{StaticResource TopbarCloseButton}"/>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <UserControl Grid.Column="1" x:Name="v_ContentDisplay"/>
            
            <panels:ExplorerView
                Grid.Column="0"
                x:Name="v_Explorer"
                Background="{DynamicResource SecondaryBackgroundColor}"
                BorderThickness="0"
                SelectedItemChanged="Explorer_OnSelectedItemChanged">
                
                <panels:ExplorerViewItem Header="Appearance/Behavior" IsSelected="True">
                    <windows:SettingsWindow.SectionTemplate>
                        <ControlTemplate>
                            <ControlTemplate.Resources>
                                <ObjectDataProvider
                                    x:Key="Skins"
                                    ObjectType="{x:Type classes:SkinHelper}"
                                    MethodName="GetSkinFiles"/>
                            </ControlTemplate.Resources>
                            
                            <Grid>
                                <subControls:SettingsCategoryControl HeaderText="Theme">
                                    <ComboBox
                                        Background="{StaticResource SecondaryBackgroundColor}"
                                        ItemsSource="{Binding
                                            Source={StaticResource Skins}}"
                                        ItemContainerStyle="{StaticResource TextStyle}"
                                        SelectionChanged="Skins_OnSelectionChanged">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate DataType="io:FileInfo">
                                                <TextBlock Text="{Binding Name}"/>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                </subControls:SettingsCategoryControl>
                            </Grid>
                        </ControlTemplate>
                    </windows:SettingsWindow.SectionTemplate>
                </panels:ExplorerViewItem>
                
                <panels:ExplorerViewItem Header="Performance">
                    <windows:SettingsWindow.SectionTemplate>
                        <ControlTemplate />
                    </windows:SettingsWindow.SectionTemplate>
                </panels:ExplorerViewItem>
                
                <panels:ExplorerViewItem Header="Keybinds">
                    <windows:SettingsWindow.SectionTemplate>
                        <ControlTemplate />
                    </windows:SettingsWindow.SectionTemplate>
                </panels:ExplorerViewItem>
            </panels:ExplorerView>
        </Grid>
    </Grid>
</windows:ModifiedWindow>
