<Window x:Class="TempoIDE.Windows.ErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:TempoIDE.Windows"
        mc:Ignorable="d"
        Title="Error" Height="180" Width="350"
        Style="{StaticResource ModalWindowStyle}"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <TextBlock 
                Text="{Binding
                    Path=Title, 
                    RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                FontSize="15"/>
            
        </Grid>
        
        <TextBlock Grid.Row="2" Name="ErrorBlock" FontSize="17"/>
        
        <Border Grid.Row="3"  BorderThickness="0">
            <Button 
                Content="Ok"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Right"
                HorizontalContentAlignment="Center"
                Click="OkButton_OnClick"/>
        </Border>
    </Grid>
</Window>
