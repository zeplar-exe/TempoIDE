<Grid x:Class="TempoIDE.UserControls.Panels.EditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TempoIDE.UserControls"
             xmlns:panels="clr-namespace:TempoIDE.UserControls.Panels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.055*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <panels:EditorTabControl
            x:Name="Tabs"
            Grid.Row="1"
            Background="{DynamicResource SecondaryBackgroundColor}"
            SelectedTabColor="CornflowerBlue"
            HoveredTabColor="LightSlateGray"
            UnselectedTabColor="Transparent" />
    </Grid>
</Grid>
